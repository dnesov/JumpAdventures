extends Label


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	# var major = ProjectSettings.get("application/config/version/major")
	# text = "{major}".format({major: major})
	# print(text)
	pass
